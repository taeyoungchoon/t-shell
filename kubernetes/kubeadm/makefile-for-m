help:
	@cat makefile | grep ^[a-z]
start:
	vagrant up &>/dev/null
check:
	vagrant box list
	vagrant status | grep \(
ssh:
	vagrant ssh
stop:
	vagrant halt &>/dev/null
clean:
	vagrant destroy -f &>/dev/null
