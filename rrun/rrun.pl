#!/usr/bin/perl

use strict;
use warnings;
use feature qw/ say switch /;
use Term::ANSIColor;

print @ARGV;

__END__
build request directory
makefile and all to do something as single command, make
output with extension 'out'

request directory as argment
hosts
target
serviceping
scp -r template to remote:/tmp/rrun-req-idx/
ssh target cd then fire
scp out to local directory with 'out'


